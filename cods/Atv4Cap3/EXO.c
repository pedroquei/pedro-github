#include <stdio.h>
#include <stdlib.h>
#include <math.h>

void main(){

    float A, B, C, D, R, S;
    printf("Insira o valor de A: ");
    scanf("%f", &A);
    printf("Insira o valor de B: ");
    scanf("%f", &B);
    printf("Insira o valor de C: ");
    scanf("%f", &C);
    R = A*C;
    S = B+D;
    printf("O resultado das operações é : %.2f , %.2F", R, S);

}  